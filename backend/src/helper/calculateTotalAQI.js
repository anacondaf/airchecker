const PollutantScaleModel = require("../models/PollutantScale");

const subIndexFormula = () => {
    
}

/**
 *
 * @param {Uint16Array} currConcentrations
 */
const calcSubIndex = (currConcentrations) => {
	let aqiIndex;

	for (const concentration of currConcentrations) {
        
    
    }
};
